fastapi==0.136.3
uvicorn==0.49.0
SQLAlchemy==2.0.50
pydantic==2.13.4
pydantic-settings==2.10.1
python-dotenv>=1.0.0
APScheduler==3.11.0
httpx==0.28.1
pytest==8.4.1
pytest-asyncio==1.1.0
passlib==1.7.4
bcrypt==4.2.1
python-jose==3.5.0
PyJWT==2.10.1
requests>=2.32.5
beautifulsoup4==4.13.5
selenium==4.35.0
langgraph==1.2.2
langgraph-checkpoint==4.1.1
langgraph-checkpoint-sqlite==3.1.0
langgraph-prebuilt==1.1.0
langgraph-sdk==0.3.15
langchain-core==1.4.1
langchain-openai==1.2.2
langchain-community==0.4.2
langchain-classic==1.0.7
langchain-text-splitters==1.1.2
langchain-protocol==0.0.15
chromadb==1.0.20
redis==6.4.0
pytz==2025.2
mcp==1.9.4


# Tavily 网页搜索 MCP Server（Python 版 / 方法2）：构建期预装进镜像，
# 运行时直接执行镜像内的 tavily-mcp 二进制，无需联网下载、不依赖 Node。
# 建议首次装通后在 CI/构建机确认版本，再在此锁定（如 tavily-mcp==x.y.z）。
# tavily-mcp==0.2.3
# 主后端 venv 只需要 mcp==1.9.4（用来和外部 MCP Server 通信）；
# tavily-mcp 作为 外部独立进程，通过 TAVILY_MCP_COMMAND 用 npx -y tavily-mcp / uvx tavily-mcp 拉起，不污染后端 venv。
# MCP server 依赖的异步 HTTP 运行时（SSE 传输方式需要）
dynaconf==3.2.13
numpy==2.3.5
pandas==2.3.3
